# sample-node

Minimal Node.js + TypeScript project for `project-parser` demos.

```bash
../../bin/project-parser report . --config ../parser.yaml
../../bin/project-parser graph . --config ../parser.yaml --format svg
```
