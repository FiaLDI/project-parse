# sample-go

Minimal Go project used to demonstrate `project-parser` on a Clean Architecture layout.

```bash
../../bin/project-parser report . --config ../parser.yaml
../../bin/project-parser graph . --config ../parser.yaml
```
